rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;  // For development - change this for production!
      allow write: if true;  // For development - change this for production!
    }
  }
} 